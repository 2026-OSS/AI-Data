# YOLOv5 v2 Artifacts

Place the trained YOLOv5 v2 handoff files in this directory.

Tracked handoff files:

- `best.pt`: trained model weights for backend inference
- `data.yaml`: dataset class names and paths used during training

Other training outputs, such as logs and metric images, should stay out of git.
